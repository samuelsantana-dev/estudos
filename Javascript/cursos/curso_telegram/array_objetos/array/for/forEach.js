const basquete = ['cesta', 'jogador',  'torcida', 'treinador', 'estadio']
basquete.forEach((value, index, array) => {
    console.log(`${index}. ${value} ---- ${array}`)
})

// O meu index é a minha numeraçao
// O meu array pega ele por completo
// O value pega o valor de cada um
// O  forEach é feito para mostrar cada um 