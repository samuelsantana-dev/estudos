function Button(props){
    return <button onClick={props.event}> {props.text}</button>
}

/*
 * o event em minusculo conecta com o do evento aqui é para mostrar o que vai fazer é prar retornar os resultados do event e so para exportar nao precisa importar nada
 */ 

export default Button