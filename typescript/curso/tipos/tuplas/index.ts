let person: [string, number] = ["John", 30];

// Representa uma lista ordenada de valores de tipos específicos.

let pessoa: [string, number] = ["João Silva", 30];
let data: [number, number, number] = [2023, 12, 10];


let product: [string, number, boolean] = ["Laptop", 1200, true]; // Tupla com tipos diferentes

// person[0] = "Jane"; // Erro de compilação: tuplas são imutáveis
